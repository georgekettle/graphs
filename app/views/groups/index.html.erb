<%= render 'components/section_header', title: 'Groups', buttons: [render('components/icon_button', link:new_group_path, icon:'plus', classes: 'icon-btn-primary')] %>

<%= render 'groups', groups: @groups %>
